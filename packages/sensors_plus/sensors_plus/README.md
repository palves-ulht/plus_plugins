# sensors_plus (with sampling rate set to 50Hz)

This is a fork of https://github.com/fluttercommunity/plus_plugins with just one modification
on the sensors_plus to set the sampling rate of the accelerometer to 50 Hz (20 ms delay).

The default sampling rate is 5 Hz (200 ms delay) which is insufficient for some applications

